spring:
  application:
    name: Main
  datasource:
    url: jdbc:h2:mem:parkingdb
    username: sa
    password: 1234
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
