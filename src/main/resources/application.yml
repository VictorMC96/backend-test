spring:
  datasource:
    url: jdbc:h2:mem:estacionamiento
    driver-class-name: org.h2.Driver
    username: sa
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console